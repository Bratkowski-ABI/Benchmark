Multithreading/Multiprocessing benchmark

Using function calculate(parameter)
    for every number from 1 till parameter add result of ((number-i)*i)

Numbers for calculation depending from sytuation will be taken from data directory or hardcoded in program.
Report will be generated in report directory.
